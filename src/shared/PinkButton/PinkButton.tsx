import React from 'react';
import S_PinkButton from './PinkButton.module.css';


const PinkButton = () => {
    return (
        <button className={S_PinkButton.PinkButton}>
            Download Resume
        </button>
    );
};

export default PinkButton;